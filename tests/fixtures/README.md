HLTV public JSON captured 2026-09-11 from `/fantasy/json` and
`/fantasy/650/overview/json`. Fixtures retain the fields used by the adapter;
leaderboards, usernames, advertising, images, and unrelated fields were removed.
The overview is deliberately a featured subset, not a complete draft pool.
